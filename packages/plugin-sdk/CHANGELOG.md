# @jstorrent/plugin-sdk Changelog

## [0.1.0]

Initial release.

- Plugin validation (`validatePlugin`)
- Test context (`createTestContext`) with fetch and HTML parsing
- CLI tool (`jstorrent-plugin validate`, `jstorrent-plugin test`)
- Internet Archive reference plugin with tests
